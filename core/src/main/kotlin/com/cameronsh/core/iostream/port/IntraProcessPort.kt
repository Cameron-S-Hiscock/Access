package com.cameronsh.core.iostream.port

import com.cameronsh.utils.Id
import java.util.UUID

import com.cameronsh.core.iostream.port.NativePort
import com.cameronsh.core.iostream.port.PortStatus.*

/*class IntraProcessPort(
    hostId: UUID,
    iostreamId: UUID,
): NativePort {
    override val id: UUID = Id.genId(this)
    override val status = OPEN
    override val host = hostId
    override val iostream = iostreamId
}*/
